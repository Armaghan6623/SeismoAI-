[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "seismoai-viz"
version = "0.1.0"
description = "Visualization module for seismic data"
dependencies = [
    "matplotlib",
    "numpy",
]