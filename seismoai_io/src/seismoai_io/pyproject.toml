[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "seismoai-io"
version = "0.1.0"
description = "IO module for seismic data processing"
dependencies = [
    "segyio",
    "numpy",
]